# MedrixFlow 项目上下文

## 项目信息
- **路径**: `REDACTED_PATH/Desktop/medrix-flow`
- **来源**: 基于 ByteDance DeerFlow 二次开发，已完成品牌定制
- **GitHub**: `https://github.com/Citrus-bit/medrix-flow`
- **本地访问**: `http://localhost:1000`

## 技术栈
- **后端**: Python 3.12, FastAPI, LangGraph, LangChain, uv
- **前端**: Next.js 16, React 19, TypeScript, TailwindCSS 4, pnpm
- **部署**: Docker + nginx 反向代理

## 架构（4 个服务）
| 服务 | 端口 | 说明 |
|------|------|------|
| LangGraph | 2024 | Agent 编排引擎 (lead_agent) |
| Gateway API | 8001 | FastAPI 网关 |
| Frontend | 3000 | Next.js (Turbopack) |
| Nginx | **1000** | 统一反向代理入口 |

## 启动/停止
```bash
make dev-daemon   # 后台启动所有服务
make stop         # 停止所有服务
```

## 本次会话已完成的改动

### 1. 端口 2026 → 1000
改动文件：`docker/nginx/nginx.local.conf`, `docker/nginx/nginx.conf`, `docker/docker-compose.yaml`, `docker/docker-compose-dev.yaml`, `scripts/serve.sh`, `scripts/start-daemon.sh`, `frontend/src/core/config/index.ts`

### 2. 首页自动跳转 /workspace
`frontend/src/app/page.tsx` 改为 `redirect("/workspace")`

### 3. 菜单精简（左下角"设置和更多"）
- 删除"访问 MedrixFlow 官方网站"
- "在 GitHub 上查看"链接改为 `https://github.com/Citrus-bit/medrix-flow`
- 删除"报告问题"和"联系我们"
- 改动文件：`workspace-nav-menu.tsx`, `types.ts`, `zh-CN.ts`, `en-US.ts`

### 4. 右上角 GitHub 图标删除
`workspace-container.tsx` 移除了 header 右侧的 GitHub 链接和 Tooltip

### 5. "关于 MedrixFlow" 内容重写
`settings/about-content.ts` 全部重写，不再出现原文标志性语句

### 6. 浅色主题改为纯白背景
`globals.css` 中 `:root` 的 `--background`, `--card`, `--popover`, `--sidebar` 等色值从微灰（`oklch(0.9855 0.0098 87.47)`）改为纯白（`oklch(1 0 0)`）

## 关键配置文件
- `config.yaml` — 主配置（模型 API key 等）
- `.env` — 环境变量（含 `HUAWEI_MAAS_API_KEY`，已配置 GLM-5 模型）
- `backend/langgraph.json` — graph 入口 `medrix_flow.agents:make_lead_agent`
- `backend/pyproject.toml` — 包名 `medrix-flow`

## 注意事项
- demo 数据（`frontend/public/demo/threads/`）中保留原 DeerFlow 线程数据
- README 等文档中仍有 `your-org/medrix-flow` 占位符未替换为实际地址
- `medrix_flow.tech` 域名在部分脚本/文档中被引用，非真实域名
