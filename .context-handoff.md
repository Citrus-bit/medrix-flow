# MedrixFlow 项目构建 - 上下文记录

## 日期
2026-03-25

## 项目位置
- 源项目: `REDACTED_PATH/Desktop/deer-flow`
- 新项目: `REDACTED_PATH/Desktop/medrix-flow`

## 已完成的工作

### 1. 项目复制
- 使用 `rsync` 从 deer-flow 复制到 medrix-flow，排除了 `.git`, `node_modules`, `.venv`, `.next`, `__pycache__`, `.langgraph_api`, `uv.lock`, `pnpm-lock.yaml`
- 共 646 个文件

### 2. Python 包重命名
- `backend/packages/harness/deerflow/` → `backend/packages/harness/medrix_flow/`

### 3. 文件/目录重命名
- `deer-flow.code-workspace` → `medrix-flow.code-workspace`
- `frontend/public/images/deer.svg` → `frontend/public/images/medrix.svg`
- `skills/public/claude-to-deerflow/` → `skills/public/claude-to-medrixflow/`
- `research_deerflow_20260201.md` → `research_medrixflow_20260201.md`

### 4. 品牌全量替换（233 个文件）
替换顺序（避免重复替换）:
1. `DEER_FLOW` → `MEDRIX_FLOW`
2. `DEERFLOW` → `MEDRIXFLOW`
3. `DeerFlow` → `MedrixFlow`
4. `Deerflow` → `Medrixflow`
5. `deerflow-harness` → `medrix-flow-harness`
6. `deer-flow` → `medrix-flow`
7. `deer_flow` → `medrix_flow`
8. `deerflow` → `medrix_flow`

额外替换:
- 🦌 → 🧬
- `Deep Exploration and Efficient Research Flow` → `Medical Research Intelligence and Data-driven eXploration Flow`
- 粗体首字母缩写也已更新
- GitHub URL `bytedance/deer-flow` → `your-org/medrix-flow`（README 中）
- `deer.svg` → `medrix.svg`（hero.tsx 中的引用）

### 5. Logo/Favicon
- `medrix.svg`: DNA 双螺旋主题 SVG（渐变绿→蓝→紫，中心 "M" 字母）
- `favicon.ico`: 使用 Python PIL 生成，16/32/48/64px 多尺寸 ICO，绿色圆形背景 + 白色 "M"

### 6. Git 初始化
- 已 `git init` 并提交初始 commit: `"Initial commit: MedrixFlow project (based on DeerFlow architecture)"`

### 7. 依赖安装
- Backend: `uv sync` ✅（在 backend/ 目录下）
- Frontend: `pnpm install` ✅（在 frontend/ 目录下）

### 8. 验证结果
- Python 导入: `medrix_flow`, `medrix_flow.config`, `medrix_flow.agents`, `MedrixFlowClient` 全部正常
- Backend pytest: `test_config_version.py` + `test_harness_boundary.py` 共 7 个测试全通过
- Frontend TypeScript: `tsc --noEmit` 通过
- 品牌审计: `grep -rl "deerflow|deer-flow|deer_flow|DeerFlow|DEER_FLOW"` 在源文件中返回空（零残留）

## 关键配置文件位置
- `backend/pyproject.toml` — name: `medrix-flow`
- `backend/packages/harness/pyproject.toml` — name: `medrix-flow-harness`, wheel packages: `medrix_flow`
- `backend/langgraph.json` — graphs: `medrix_flow.agents:make_lead_agent`
- `frontend/package.json` — name: `medrix-flow-frontend`
- `config.example.yaml` — 顶部注释: MedrixFlow application
- `Makefile` — 帮助文本: MedrixFlow Development Commands

## 项目启动方式
```bash
cd REDACTED_PATH/Desktop/medrix-flow
make config    # 生成 config.yaml（如果还没有）
make install   # 安装依赖（已安装可跳过）
make dev       # 启动开发服务 → http://localhost:2026
```

## 注意事项
- `.env` 文件中有一个 `HUAWEI_MAAS_API_KEY`，是从原项目带过来的，可能需要更新
- README 中的 GitHub URL 已替换为 `your-org/medrix-flow`，需要替换为实际的 GitHub 组织/用户名
- demo 数据（`frontend/public/demo/threads/`）中的 `bytedance` 引用保留在 JSON 线程数据里（这是缓存的演示内容，不影响功能）
- `medrix_flow.tech` 域名在一些地方被引用（从 `deerflow.tech` 替换而来），这是一个不存在的域名，需要时可更新
- 项目功能完整性取决于配置正确的 LLM API key（在 `.env` 或 `config.yaml` 中配置）

## 技术栈
- **后端**: Python 3.12+, FastAPI, LangGraph, LangChain, uv (包管理)
- **前端**: Next.js 16+, React 19, TypeScript, TailwindCSS 4, pnpm
- **部署**: Docker + Docker Compose, nginx 反向代理
- **Agent 框架**: LangGraph (lead_agent → sub-agents), sandbox 执行环境
