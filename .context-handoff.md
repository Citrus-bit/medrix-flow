# MedrixFlow 项目上下文

## 项目信息
- **路径**: `REDACTED_PATH/Desktop/medrix-flow`
- **GitHub**: `https://github.com/Citrus-bit/medrix-flow`
- **本地访问**: `http://localhost:1000`

## 技术栈
- **后端**: Python 3.12, FastAPI, LangGraph, LangChain, uv
- **前端**: Next.js 16, React 19, TypeScript, TailwindCSS 4, Shadcn UI, pnpm
- **状态管理**: TanStack React Query（服务端状态）+ localStorage（本地设置）
- **部署**: Docker + nginx 反向代理

## 架构（4 个服务）
| 服务 | 端口 | 说明 |
|------|------|------|
| LangGraph | 2024 | Agent 编排引擎 (lead_agent) |
| Gateway API | 8001 | FastAPI 网关 (`backend/app/gateway/`) |
| Frontend | 3000 | Next.js (Turbopack) |
| Nginx | **1000** | 统一反向代理入口 |

## 启动
```bash
双击 start.command          # 一键启动所有服务 + 自动打开浏览器
make dev                    # 或手动启动
make dev-daemon             # 后台启动
make stop                   # 停止所有服务
```

## 关键配置文件
| 文件 | 用途 |
|------|------|
| `config.yaml` | 主配置：模型定义、工具、sandbox、skills 等（`$VAR` 引用 .env） |
| `.env` | 环境变量：`HUAWEI_MAAS_API_KEY`, `TAVILY_API_KEY`, `JINA_API_KEY` |
| `config.example.yaml` | 配置模板（包含所有可用字段示例） |
| `backend/langgraph.json` | graph 入口 `medrix_flow.agents:make_lead_agent` |
| `backend/pyproject.toml` | 包名 `medrix-flow` |

## 配置加载链路
```
.env (load_dotenv) -> config.yaml (yaml.safe_load)
  -> AppConfig.resolve_env_variables（递归替换 $VAR）
  -> AppConfig 单例缓存（mtime 自动热重载）
  -> Gateway API 通过 get_app_config() 读取
```
核心文件: `backend/packages/harness/medrix_flow/config/app_config.py`

---

## 前端架构要点

### 路由结构
```
/                           -> 重定向 /workspace
/workspace                  -> 重定向 /workspace/chats
/workspace/chats/[thread_id] -> 聊天页面
/workspace/agents/...       -> Agent 页面
```

### 设置系统
- **Settings Dialog**: `frontend/src/components/workspace/settings/settings-dialog.tsx`
- 左侧导航 + 右侧内容，标签页：配置 / 外观 / 通知 / 记忆 / 工具 / 技能 / 关于
- 每个标签页独立组件，使用 `SettingsSection` 包装器
- 本地设置: `core/settings/local.ts` -> localStorage `medrix_flow.local-settings`

### API 调用模式
```typescript
const { data, isLoading, error } = useQuery({ queryKey: [...], queryFn: () => fetch(...) });
const { mutate } = useMutation({ mutationFn: ..., onSuccess: () => invalidateQueries(...) });
```
参考实现: `core/mcp/hooks.ts` (GET/PUT 完整 CRUD 模式)

### 国际化
- 类型定义: `core/i18n/locales/types.ts`
- 英文: `core/i18n/locales/en-US.ts`
- 中文: `core/i18n/locales/zh-CN.ts`
- Hook: `useI18n()` -> `t.xxx.yyy`

### UI 组件库
- 基础组件: `components/ui/` (Button, Input, Select, Switch, Dialog, ScrollArea...)
- 复合组件: `components/workspace/` (Sidebar, InputBox, Settings...)
- AI 组件: `components/ai-elements/` (ModelSelector, Reasoning...)
- Toast: `sonner` 库 (`toast.success(...)`, `toast.error(...)`)

---

## 后端 Gateway API 端点

| 路由 | 方法 | 文件 |
|------|------|------|
| `/api/models` | GET | `routers/models.py` |
| `/api/mcp/config` | GET/PUT | `routers/mcp.py` |
| `/api/mcp/test` | POST | `routers/mcp.py` |
| `/api/memory` | GET | `routers/memory.py` |
| `/api/skills` | GET/PUT | `routers/skills.py` |
| `/api/setup/config` | GET | `routers/setup.py` |
| `/api/setup/models` | PUT | `routers/setup.py` |
| `/api/setup/test-model` | POST | `routers/setup.py` |
| `/api/setup/test-tool-key` | POST | `routers/setup.py` |
| `/health` | GET | `app.py` |

### 模型配置 Schema
`backend/packages/harness/medrix_flow/config/model_config.py`:
- `name`, `display_name`, `description`, `use`（提供商类路径）, `model`
- `api_key`, `base_url`, `max_tokens`, `temperature`
- `supports_thinking`, `supports_vision`, `supports_reasoning_effort`
- `ConfigDict(extra="allow")` — 允许提供商特有字段

### Setup API Schema（前端 <-> 后端）
`backend/app/gateway/routers/setup.py` + `frontend/src/core/setup/types.ts`:
- **ModelSetupItem**: `name`, `provider`, `model`, `base_url`, `api_key`, `api_key_env_var`, `max_tokens`, `temperature`, `supports_thinking`, `supports_vision`
- 无 `display_name` 字段 — 后端保存时自动用 `model` 值填充 config.yaml 的 `display_name`
- GET 返回**完整真实 API Key**（非 masked），前端用密码框 + 眼睛按钮控制可见性

### 工具 API Key 来源
| 工具 | Key 来源 | 代码 |
|------|---------|------|
| Tavily | `config.yaml` tools 段 `api_key: $TAVILY_API_KEY` -> model_extra | `community/tavily/tools.py` |
| Jina | `os.getenv("JINA_API_KEY")` 直接读环境变量 | `community/jina_ai/jina_client.py` |

---

## 配置设置页（Setup Settings Page）

设置对话框中的"配置"标签页，用户可从前端 UI 直接配置模型和工具 API Key。

### 相关文件
| 文件 | 用途 |
|------|------|
| `backend/app/gateway/routers/setup.py` | 4 个 API 端点：读取配置、保存模型配置、测试模型连通性、测试工具 Key |
| `backend/app/gateway/app.py` | 注册 setup router |
| `frontend/src/core/setup/types.ts` | TypeScript 类型定义 |
| `frontend/src/core/setup/api.ts` | fetch 封装 |
| `frontend/src/core/setup/hooks.ts` | React Query hooks |
| `frontend/src/core/setup/index.ts` | 模块导出 |
| `frontend/src/components/workspace/settings/setup-settings-page.tsx` | UI：模型卡片表单 + 工具 Key 表单 + 测试/保存按钮 |

### 模型卡片表单字段
- **模型** — 单一输入框，用户填写模型名（如 `gpt-4o`），同时作为 `name` 和 `model`
- **提供商** — Select 下拉，每个选项有唯一 `key`（openai / anthropic / google / deepseek / openai-compatible）
  - 选中状态用 ModelCard 内部 `presetKey` state 追踪，不从 provider 反推
  - 底层 `provider` 字段存储提供商类路径（如 `langchain_openai:ChatOpenAI`）
- **Base URL** / **API Key** / **Max Tokens** / **Temperature**
- **Thinking** / **Vision** 开关
- **测试按钮** — 动态实例化 provider 类发送 `ainvoke("Hi")`
- **API Key 眼睛按钮** — 输入框右侧外部 flex 布局，切换 password/text 类型

### 工具 Key 卡片
- Tavily / Jina 各一个密码输入框 + 眼睛按钮 + 测试按钮

### 保存流程
模型定义写入 `config.yaml`，API Key 写入 `.env`（config.yaml 中引用为 `$VAR`），自动 `reload_app_config()` 热重载

### Provider 预设（PROVIDER_PRESETS）
```typescript
[
  { key: "openai",            label: "OpenAI",            provider: "langchain_openai:ChatOpenAI" },
  { key: "anthropic",         label: "Anthropic",         provider: "langchain_anthropic:ChatAnthropic" },
  { key: "google",            label: "Google Gemini",     provider: "langchain_google_genai:ChatGoogleGenerativeAI" },
  { key: "deepseek",          label: "DeepSeek",          provider: "medrix_flow.models.patched_deepseek:PatchedChatDeepSeek" },
  { key: "openai-compatible", label: "OpenAI Compatible", provider: "langchain_openai:ChatOpenAI" },
]
```
注意：OpenAI 和 OpenAI Compatible 共享相同 provider 类路径，靠 `presetKey` state 区分。

---

## 首次访问自动弹出配置提醒

**文件:** `frontend/src/components/workspace/workspace-nav-menu.tsx`

- 新开标签页/窗口 -> 自动弹出配置页（defaultSection="setup"）
- 通过 `sessionStorage`（key: `medrix_flow.setup-prompted`）确保同一浏览器会话内只触发一次
- 刷新页面不重复弹出，关闭标签页后重新打开会再次弹出

---

## 流式传输 & 线程持久化架构

### 流式传输管线
| 组件 | 说明 |
|------|------|
| `useStream` (SDK) | `@langchain/langgraph-sdk` React hook，管理 SSE 连接 |
| `reconnectOnMount: true` | 用 `sessionStorage` 存 `lg:stream:{threadId}` -> `runId` 映射 |
| `streamResumable: true` | 断连后后端继续运行（`onDisconnect: "continue"`） |
| 乐观消息 | `thread.messages.length > prevMsgCountRef.current` 时清除 |

### 线程列表查询
- `useThreads()` -> `threads.search()` POST `/threads/search`
- 默认参数: `limit: 50, sortBy: "updated_at", select: ["thread_id", "updated_at", "values", "status"]`
- `refetchOnWindowFocus: true`, `staleTime: 30_000`
- 新线程在 `onCreated` 回调中 optimistic 插入 query cache
- `visibilitychange` 监听器在 tab 恢复可见时 invalidate 缓存

### 思考/推理展示
- 发送消息后立即显示 `<Reasoning isStreaming={true}>` 组件（带脑图标 + "Thinking..." shimmer + 实时计秒）
- 由 `message.additional_kwargs._thinking` 标识触发（`message-list-item.tsx`）
- 真实推理内容到达后，由 `hasReasoning()` + `extractReasoningContentFromMessage()` 接管渲染

### 子代理任务展示
- `message-list.tsx`: 检测 `assistant:subagent` 消息组，从 `task` tool_calls 创建 `Subtask` 对象
- `subtask-card.tsx`: ChainOfThought 卡片，shimmer 描述文字、ShineBorder 动画、实时 tool_call 进度
- `task_running` 自定义事件通过 `onCustomEvent` -> `useUpdateSubtask()` 更新 SubtaskCard
- `core/tasks/context.tsx`: SubtaskContext 管理任务状态，`setTasks` 仅在 `latestMessage` 存在时触发重渲染

### 关键文件索引（流式/线程相关）
| 文件 | 用途 |
|------|------|
| `frontend/src/core/threads/hooks.ts` | useThreadStream, useThreads, sendMessage, optimistic 消息 |
| `frontend/src/core/threads/utils.ts` | titleOfThread, pathOfThread, textOfMessage |
| `frontend/src/core/threads/types.ts` | AgentThread, AgentThreadState, AgentThreadContext |
| `frontend/src/components/workspace/messages/message-list.tsx` | 消息分组、子代理渲染、StreamingIndicator |
| `frontend/src/components/workspace/messages/message-list-item.tsx` | 单条消息渲染：thinking/reasoning/task 分支 |
| `frontend/src/components/workspace/messages/subtask-card.tsx` | 子代理任务卡片 UI |
| `frontend/src/components/ai-elements/reasoning.tsx` | Reasoning 组件（计时器 + 自动折叠） |
| `frontend/src/core/tasks/context.tsx` | SubtaskContext, useUpdateSubtask |
| `frontend/src/components/workspace/recent-chat-list.tsx` | 侧边栏线程列表 |
| `frontend/src/components/workspace/chats/use-thread-chat.ts` | threadId 生成（new -> uuid） |
| `backend/packages/harness/medrix_flow/models/claude_provider.py` | ClaudeChatModel thinking budget（80% max_tokens） |

---

## 历史改动

1. 端口 2026 -> 1000（nginx/docker/config）
2. 首页自动跳转 /workspace
3. 菜单精简（左下角"设置和更多"）
4. 右上角 GitHub 图标删除
5. "关于 MedrixFlow" 内容重写
6. 浅色主题改为纯白背景
7. Thinking/Reasoning 流式显示修复
   - `frontend/src/core/threads/hooks.ts` — 乐观 thinking 占位消息 content 改为 `t.common.thinking`
   - `frontend/src/core/messages/utils.ts` — `hasContent()` 的 array 分支检查真正的 text/image 元素
8. 一键启动脚本 `start.command`
9. 配置设置页（Setup Settings Page）— 前后端完整实现
10. 模型配置简化 — 移除 `display_name`/`modelId` 字段，用户只需填写一个"模型"字段
11. Provider Select 修复 — 每个预设用唯一 `key` 标识，修复重复 value 导致的多选和无法切换问题
12. API Key 可见性 — 后端返回完整真实 Key，前端密码框 + 眼睛按钮切换显示
13. 线程消失 + Safari 修复 — useThreads 加 refetchOnWindowFocus/staleTime/status select、onCreated optimistic 插入、visibilitychange 监听、titleOfThread 防空
14. 思考状态展示增强 — 乐观 thinking 消息改用 `<Reasoning isStreaming>` 组件（脑图标 + shimmer + 实时计秒），替代静态 spinner

### 性能优化（启动速度 + 运行时）

15. **P0-1: 并行化 serve.sh / start-daemon.sh** — LangGraph、Gateway、Frontend 三个服务同时启动，统一等待循环，仅 Nginx 在三端口就绪后启动。预计启动提速 40-60%。
    - 改动文件: `scripts/serve.sh`, `scripts/start-daemon.sh`
16. **P0-2: 前端重型库懒加载**
    - `frontend/src/components/ai-elements/code-block.tsx` — shiki 的 `codeToHtml` 改为 `await import("shiki")` 动态导入，类型 import 保持静态
    - `frontend/src/components/workspace/code-editor.tsx` — 完全重写：10 个 CodeMirror 包（7 语言包 + 2 主题 + react-codemirror）用 `next/dynamic` + `{ ssr: false }` 包裹，内部组件通过 `Promise.all()` 闭包接收所有依赖
17. **P1-1: 移除 nuxt-og-image** — Next.js 项目中的 Nuxt 依赖，零代码引用。从 `frontend/package.json` 移除，pnpm install 更新 lockfile
18. **P1-2: 移除未使用 Python 依赖** — `kubernetes` 和 `duckdb` 在后端零代码引用（约 130MB）。从 `backend/packages/harness/pyproject.toml` 移除，uv sync 卸载 7 个包
19. **P2: config-upgrade.sh 快速跳过** — 添加 bash 级 `grep` 对比 `config_version`，版本一致时直接退出不启动 Python 进程
    - 改动文件: `scripts/config-upgrade.sh`

### Bug 修复

20. **max_tokens 200000 -> 131072** — GLM-5 通过华为 ModelArts 最大支持 131072 tokens，原 200000 导致 400 BadRequest 被前端静默吞掉
    - 改动文件: `config.yaml`
21. **消息发送期间追加消息导致 UI 卡死** — `sendMessage()` 中 `sendInFlightRef.current` 为 true 时原来直接 `return` 丢弃第二条消息，现在改为先 `await thread.stop()` 取消当前运行再发送新消息
    - 改动文件: `frontend/src/core/threads/hooks.ts`
22. **对话滚动看不到用户第一条提问** — 聊天页 header `h-12`(48px) 但消息列表 padding-top 仅 `pt-10`(40px)，差 8px 导致首条消息被遮挡。修复: `pt-10` -> `pt-12`
    - 改动文件: `frontend/src/app/workspace/chats/[thread_id]/page.tsx`
23. **MCP 工具管理功能完善** — 原 UI 仅支持 enable/disable 切换，现完整实现 CRUD + 连接测试
    - 后端新增 `POST /api/mcp/test` 端点（stdio 验证命令、sse/http 验证 URL 连通）
    - 前端 types/api/hooks 全面扩展（MCPServerConfig 完整字段、testMCPServer API、useSaveMCPConfig/useTestMCPServer hooks）
    - ToolSettingsPage 完全重写：服务器卡片编辑所有字段、新增/删除/重命名服务器、传输类型切换、测试连接、保存配置
    - i18n 中英文 27 个新字段
    - 改动文件: `backend/app/gateway/routers/mcp.py`, `frontend/src/core/mcp/{types,api,hooks}.ts`, `frontend/src/core/i18n/locales/{types,en-US,zh-CN}.ts`, `frontend/src/components/workspace/settings/tool-settings-page.tsx`

### 评估后取消的优化

- **Barrel files 重构** — 23 个 `index.ts` 有 `export *`，但 Turbopack 处理合理，ROI 低，取消
- **Gateway import 隔离** — 高复杂度低收益，取消
- **@xyflow/react 懒加载** — 定义在 `ai-elements/` 但无页面导入，不在关键路径上
- **Landing page (Galaxy/gsap/ogl)** — 仅用于 `/` 且立即重定向到 `/workspace`，不在关键路径上

### 从上游 DeerFlow 同步的安全修复与功能改进

上游 `bytedance/deer-flow` 自 fork 以来有 ~1770 commits，采用方案 B（选择性 cherry-pick）同步关键改动。

**Phase 1 — 安全修复**

24. **sandbox/security.py** — 移植 `uses_local_sandbox_provider()` + `is_host_bash_allowed()` 安全门控函数，防止 LocalSandboxProvider 的 host-shell 逃逸漏洞
    - 新建: `backend/packages/harness/medrix_flow/sandbox/security.py`
    - 修改: `sandbox/__init__.py` 导出安全函数
25. **SandboxConfig.allow_host_bash** — 新增布尔字段（默认 `false`），仅在完全可信的本地环境下才允许 host bash 执行
    - 修改: `backend/packages/harness/medrix_flow/config/sandbox_config.py`
26. **SandboxAuditMiddleware** — Bash 命令安全审计中间件，对每个 bash tool call 做 regex + shlex 分析，高危命令阻断、中危命令警告、全量审计日志
    - 新建: `backend/packages/harness/medrix_flow/agents/middlewares/sandbox_audit_middleware.py`
    - 修改: `agents/lead_agent/agent.py` 注册到中间件链（LoopDetection 之后、Clarification 之前）

**Phase 2 — 后端功能改进**

27. **Memory 纠正检测** — 新增 `detect_correction()` 函数，通过中英文 regex 匹配识别用户纠正意图（"你理解错了"/"try again" 等），触发记忆优先级更新。同时改进 `_extract_message_text()` 正确处理 list[str|dict] 混合 content 格式，thread_id 解析增加 `get_config()` fallback，`print()` 日志替换为 `logger.debug()`
    - 修改: `backend/packages/harness/medrix_flow/agents/middlewares/memory_middleware.py`
    - 修改: `backend/packages/harness/medrix_flow/agents/memory/queue.py`（`ConversationContext` + `add()` 新增 `correction_detected` 参数）
28. **TokenUsageMiddleware** — 每轮 LLM 调用后记录 input/output/total token 用量到日志
    - 新建: `backend/packages/harness/medrix_flow/agents/middlewares/token_usage_middleware.py`
    - 新建: `backend/packages/harness/medrix_flow/config/token_usage_config.py`
    - 修改: `agents/lead_agent/agent.py` 注册到中间件链
29. **MemoryStorage 存储抽象** — 引入 `MemoryStorage` ABC + `FileMemoryStorage` 实现 + `get_memory_storage()` 工厂，支持通过 `memory.storage_class` 配置项插拔自定义存储后端
    - 新建: `backend/packages/harness/medrix_flow/agents/memory/storage.py`
    - 修改: `backend/packages/harness/medrix_flow/config/memory_config.py`（新增 `storage_class` 字段）

**Phase 3 — 前端 UX（未实施，待后续）**

- Cmd+K 命令面板、Token 用量显示 UI、对话导出 Markdown/JSON
- IME 处理评估后取消 — 现有 `useRef` + delayed reset 实现比上游 `ime.ts` 更优（正确处理 Chrome compositionend 事件顺序问题）

**中间件链更新后顺序（17 层）**:
ThreadData → Uploads → Sandbox → DanglingToolCall → (Guardrail) → ToolErrorHandling → (Summarization) → (TodoList) → Title → Memory → (ViewImage) → (DeferredToolFilter) → (SubagentLimit) → LoopDetection → **SandboxAudit** → **TokenUsage** → Clarification

---

## 当前模型运行时配置
- 模型: `glm-5` via `langchain_openai:ChatOpenAI` -> 华为 ModelArts `https://api.modelarts-maas.com/v2`
- API Key 环境变量: `$HUAWEI_MAAS_API_KEY`
- `supports_thinking: false`（glm-5 不支持 thinking 模式）
- `max_tokens: 131072`（华为 ModelArts 上限）

---

## 注意事项
- demo 数据（`frontend/public/demo/threads/`）中保留原 DeerFlow 线程数据
- README 等文档中仍有 `your-org/medrix-flow` 占位符未替换为实际地址
- `medrix_flow.tech` 域名在部分脚本/文档中被引用，非真实域名
- 前端构建需要 `BETTER_AUTH_SECRET` 环境变量（`pnpm build` 时设置）
- Setup API 返回完整 API Key（无 mask），依赖本地网络安全（localhost 访问）
- **LangGraph dev 服务器线程持久化限制**: 使用 `langgraph_runtime_inmem`，线程元数据存内存 + pickle（`.langgraph_api/.langgraph_ops.pckl`），10 秒 flush 一次。服务重启或 pickle 损坏会丢失所有线程。SQLite checkpointer（`checkpoints.db`）只存 checkpoint 数据不存线程元数据。生产环境应使用 PostgreSQL 后端。
- **前端日志中的 `Dropped unsupported LangGraph stream mode(s): tools` 警告**: 不影响功能，属于独立小问题
- **后端完整测试套件**（`make test`）未完全跑通（超时 3 分钟），建议手动运行 `cd backend && make test` 验证

---

## MCP 工具管理系统

### 架构

```
extensions_config.json (mcpServers + skills)
  -> ExtensionsConfig 单例缓存 (mtime 热重载)
  -> Gateway API GET/PUT/POST /api/mcp/*
  -> Frontend ToolSettingsPage (完整 CRUD + 测试)
  -> LangGraph Server 通过 mtime 检测自动重载 MCP tools
```

### 后端文件
| 文件 | 用途 |
|------|------|
| `backend/app/gateway/routers/mcp.py` | 3 个端点：GET config / PUT config / POST test |
| `backend/packages/harness/medrix_flow/config/extensions_config.py` | ExtensionsConfig 模型，mcpServers 别名，env 变量解析 |
| `backend/packages/harness/medrix_flow/mcp/client.py` | 构建 MCP 传输参数 (stdio/sse/http) |
| `backend/packages/harness/medrix_flow/mcp/tools.py` | 加载已启用 MCP 服务器的工具 |
| `backend/packages/harness/medrix_flow/mcp/cache.py` | MCP 工具缓存，config mtime 失效机制 |

### 前端文件
| 文件 | 用途 |
|------|------|
| `frontend/src/core/mcp/types.ts` | MCPServerConfig（完整字段）、MCPTransportType、MCPTestRequest/Response |
| `frontend/src/core/mcp/api.ts` | loadMCPConfig / updateMCPConfig / testMCPServer |
| `frontend/src/core/mcp/hooks.ts` | useMCPConfig / useEnableMCPServer / useSaveMCPConfig / useTestMCPServer |
| `frontend/src/components/workspace/settings/tool-settings-page.tsx` | 完整 MCP 管理 UI |

### MCPServerConfig 字段
`enabled`, `type` (stdio/sse/http), `command`, `args`, `env`, `url`, `headers`, `description`

### ToolSettingsPage UI 功能
- 服务器卡片列表：名称（可编辑重命名）、传输类型下拉、启用开关
- stdio 模式显示 command + args（多行）
- sse/http 模式显示 URL + HTTP headers
- 环境变量：textarea，KEY=VALUE 每行一个
- 新增/删除/测试连接/保存（脏状态追踪）
- 测试端点：stdio 验证命令存在并可启动，sse/http 发 GET 检查响应
